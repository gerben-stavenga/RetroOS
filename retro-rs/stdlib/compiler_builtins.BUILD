# BUILD file for compiler_builtins crate
package(default_visibility = ["//visibility:public"])

filegroup(
    name = "src",
    srcs = glob(["src/**/*.rs", "libm/src/**/*.rs"]),
)

exports_files(glob(["**/*"]))
