load("@rules_rust//rust:defs.bzl", "rust_static_library")

rust_static_library(
    name = "printmsg_lib",
    srcs = ["src/main.rs"],
    crate_name = "printmsg",
    edition = "2024",
    rustc_flags = [
        "-Cpanic=abort",
        "-Crelocation-model=static",
        "-Cforce-frame-pointers=yes",
    ],
    deps = ["//crt"],
)

genrule(
    name = "printmsg_elf",
    srcs = [":printmsg_lib", "//apps/init:user.ld"],
    outs = ["printmsg.elf"],
    cmd = "ld.lld -m elf_i386 -T $(location //apps/init:user.ld) -o $@ $(location :printmsg_lib)",
    visibility = ["//visibility:public"],
)
