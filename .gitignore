# Bazel
bazel-*
.bazelrc.user
MODULE.bazel.lock


# Build artifacts
build/
image
image.bin

# Rust analyzer (contains absolute paths, regenerate with bazel)
rust-project.json

# IDE
.vscode/
.idea/

# Proprietary binaries (not for public distribution)
apps-proprietary/

# Generated 16 MiB VIFIRET FreeDOS HDD (rebuild: test/hdpmi_vif_img/make.sh)
test/hdpmi_vif_img/vifiret-hdpmi.img

# FreeDOS install media + persistent HDD (huge binaries; user-local)
freedos/*.iso
freedos/*.ISO
freedos/*.img
freedos/*.IMG
freedos/.installed
.freedos-cache/

# Loose images / debug artifacts at repo root
tmp/
traces/
/*.wav
/*.ppm
/*.log

# Claude Code session state
.claude/

# Python bytecode (test harness)
__pycache__/

# Fetched VGA BIOS ROM (see build_vga_rom_args in run.sh)
third_party/vgabios/

# Codex CLI session state (config + nested worktrees), mirrors .claude/
.codex/

# Local game CD images (proprietary, see freedos/*.iso above)
/CD/
