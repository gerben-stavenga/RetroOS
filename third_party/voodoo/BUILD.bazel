load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")

package(default_visibility = ["//visibility:public"])

# 3dfx Voodoo Graphics (SST-1) as a passive card, derived from DOSBox
# Staging's voodoo.cpp (Aaron Giles' MAME core). See README.md.
#
# The whole RetroOS target graph uses opt-level z, including this per-pixel
# rasterizer; performance-sensitive exceptions must be justified by benchmarks.
rust_library(
    name = "voodoo",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "voodoo",
    edition = "2024",
    rustc_flags = [
        "-Cpanic=abort",
        "-Crelocation-model=static",
    ],
)

# bazelisk test //third_party/voodoo:voodoo_test --platforms=@platforms//host
rust_test(
    name = "voodoo_test",
    crate = ":voodoo",
    rustc_flags = [
        "-Cpanic=abort",
        "-Zpanic_abort_tests",
        "-Crelocation-model=static",
    ],
)
